let menuTop;
$(window).on("load", () => menuTop = $(".menu-bar").position().top);
$(window).on("scroll", function() {
    const offsetY = window.scrollY;
    if(offsetY >= menuTop) $('.menu-bar').addClass("menu-bar--fixed");
    else $('.menu-bar').removeClass("menu-bar--fixed");
});